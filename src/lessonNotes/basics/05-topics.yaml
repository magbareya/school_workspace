WriteLine vs Write

string class:
    - indexing (s[0])
    - Contains
    - IndexOf
    - Length
    - Substring
    - ToLower
    - ToUpper

Math class:
    - Math.Max
    - Math.Min
    - Math.Pow
    - Math.Sqrt
    - Math.Abs
    - Math.Round

Random class:
    - define random instance
    - Nex()
    - Next(max)
    - Next(min, max)
    - NextDouble

char:
    - define char
    - char.Parse
    - char <--> int (ASCII)
